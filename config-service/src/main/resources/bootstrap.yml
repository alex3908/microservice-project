server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alex3908/microservice-project.git
          searchPaths: config-data
          username: ${GET_USER}
          password: ${GET_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
